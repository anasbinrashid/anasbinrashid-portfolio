/** @type {import('next').NextConfig} */
const nextConfig = {
  // GitHub Pages project sites serve under: https://<user>.github.io/<repo>/
  // Your repo name is: anasbinrashid-portfolio
  basePath: "/anasbinrashid-portfolio",

  // Required for static export (GitHub Pages).
  output: "export",
};

export default nextConfig;

